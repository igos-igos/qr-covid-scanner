import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  tabBar: {
    height: 60,
    backgroundColor: '#fff',
    borderTopColor: '#fff',
    shadowColor: '#fff',
  },
});

export default styles;
